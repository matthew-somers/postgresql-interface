postgresql-interface
====================

Database interface in C using libpq. It can connect with a db, find a table, and then insert/view/delete data.

Requires -lpq option to compile.

Usage: ./pginterface dbname host pgusername pgpassword
